import styled from "styled-components";
import {color,
    layout,
    typography,
    flexbox,
    space,
    border,
shadow} from "styled-system";

export const Box = styled('div')(
    color,
    layout,
    flexbox,
    space,
    typography,
    border,
    shadow
)